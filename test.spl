



package Point {
  sub get1 () : int {
    
  }
  
  sub get2 () : static const long {
    1;
  }

  sub get3 () : int {}
  sub get4 ($foo : int) : int {}
  sub get5 ($foo : const int) : int {}
  sub get6 ($foo : static const int) : int {}

  sub get7 ($foo : int, $bar : int) : int {}
  sub get8 ($foo : const int, $bar : const int) : int {}
  sub get9 ($foo : static const int, $bar : static const int) : int {}
}

package Point3D {

  use Point -pt;

  use Point;

  sub last ($foo : int) : int { }

  # subroutine
  sub sum1 () : double {
    2;
    4;
  }
  sub sum2 ($foo : const int, $bar : const int) : const int { }

  sub sum3 ($foo : int, $bar : float) : int {
    if (3) {
    
    }
    
    if (3) {
      1;
    }
    elsif (4) {
      4;
    }
    else {
      
    }

    if (3) {
      1;
    }
    elsif (4) {
      4;
    }
    elsif (5) {
    
    }
    else {
      
    }
    
    if (1) {
      
    }
    else {
      
    }
    
    return 1;
  }

  my $title : double = 4.1567;
 
  my $title : int;
  my $title : const int;
  my $title : static const int;
  my $title : int = 1;
  my $title : int;
  my $title : int = 4;

  $title->();
  $title->(1);
  $title->(1, 2);

  ++$test;
  $test++;

  --$test;
  $test--;

  +$test;
  -$test;
  +$test - -$test;

  1 + 2;

  for (my $i : int = 0; $i < 5; $i = $i + 1) {
    1;
    last;
    next;
  }

  1 > 2;
  1 >= 2;
  1 < 2;
  1 <= 2;

  1 == 1;
  $title != 2;

  1 = 1;


  1 - 4;

  1 || 2;
  1 | 2;

  1 && 2;
  1 & 3;

  my $title : int = sub () : int { };
  my $title : int = sub ($p : int) : const int { };

  $title += 1;

  $title -= 1;

  $title *= 1;
  $title /= 2;

  $title % $title;
  $title %= $title;

  $title ^ $title;
  $title ^= $title;

  !$title;
  ~$title;

  $title ||= 1;
  $title &&= 2;

  $title << 2;
  $title >> 2;



  $test->x;
  $test->sum();
  $test->sum(1);
  $test->sum(1, 2);

  Test->sum();
  Test->sum(1);
  Test->sum(1, 2);

  $test->x = 3;
  $test->last;


  while (1) {
    1;
  }
  "abcde";
  "";

  _sum();
  sum_foo();
  sum::foo();

  $sum_::foo;

  $title->[3];
  $title->[3] = 4;

  my $title : int = true;
  my $title : int = false;

  has x : int;
  has last : int;

  has title : static int = 2;

  sum();
  sum(1, 3, 5);

  my $char : char = 'a';
}
