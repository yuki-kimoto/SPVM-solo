package Test {
  sub main($mvar : int) : int {
    my $num2 = 3L;
    
    switch($num2) {
      case 1L:
        std::printi(1);
      case 3L:
        std::printi(2);
      case 10000L:
        std::printi(2);
      default:
        std::printi(5);
    }
  }
}