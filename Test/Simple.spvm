use Test::EnumA;

package Test::Simple {
  has x : long;
  has y : long;
  
  has title : long;
  
  sub get_x ($self : Test::Simple) : long {
    return $self{x};
  }
  
  sub get2 () : long {
    1;
    my $var1 : long;
    my $var2 : long;
    
    return 0L;
  }

  sub get3 () : long { return 0L; }
  sub get4 ($foo : long) : long { return 0L; }
  sub get7 ($foo : long, $bar : long) : long { return 0L; }

  sub sum4 ($self : Test::Simple) : long {
    
    my $title : int;
   
    # $title->();
    # $title->(1L);
    # $title->(1L, 2L);

    $self{x};
    $self->sum5();

    Test::Simple::sum5($self);

    $self{x} = 3L;

    my $char : byte = 'a';
    
    return 0L;
  }
  
  sub sum5($self : Test::Simple) : long {
    $self{x};
    
    return 0L;
  }
}
