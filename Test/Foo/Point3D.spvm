package Test::Foo::Point3D {

  use Test::Point;

  has x : int;
  has last : int;

  has title : int;

  sub last ($foo : int) : int { }

  sub sum() : int { }
  sub sum($foo : int) : int { }
  sub sum($foo : int, $bar : int) : int { }
  sub sum($self : Test::Foo::Point3D, $foo : int, $bar : int) : int { }
  
  # subroutine
  sub sum1 () : double {
    2;
    4;
    "abcdefghijklnm";
  }
  sub sum2 ($foo : int, $bar : int) : int { }

  sub sum3 ($foo : int, $bar : float) : int {
    
    if (3) {
    
    }
    
    if (3) {
      1;
    }
    elsif (4) {
      4;
    }
    else {
      
    }

    if (3) {
      1;
    }
    elsif (4) {
      4;
    }
    elsif (5) {
    
    }
    else {
      
    }
    
    if (1) {
      
    }
    else {
      
    }
    
    return 1;
  }

  sub sum4 ($self : Test::Foo::Point3D) : int {
    
    my $title : int;
    my $title1 : double = 4.1567;
   
    my $title2 : int;
    my $title3 : int;
    my $title4 : int;
    my $title5 : int = 1;
    my $title6 : int;
    my $title7 : int = 4;

    # $title->();
    # $title->(1);
    # $title->(1, 2);
    
    my $test : int = 0;
    ++$test;
    $test++;

    --$test;
    $test--;

    +$test;
    -$test;
    # +$test - -$test;
        
    1 + 2;
    
    my $i : int;
    for ($i = 0; $i < 5; $i = $i + 1) {
      1;
      last;
      next;
    }

    1 > 2;
    1 >= 2;
    1 < 2;
    1 <= 2;

    1 == 1;
    $title != 2;

    1 = 1;


    1 - 4;

    1 || 2;
    1 | 2;

    1 && 2;
    1 & 3;

    $title % $title;

    $title ^ $title;

    !$title;
    ~$title;

    $title << 2;
    $title >> 2;

    $self->x;
    $test->sum();
    $test->sum(1);
    $test->sum(1, 2);

    Test::Foo::Point3D::sum();
    Test::Foo::Point3D::sum(1);
    Test::Foo::Point3D::sum(1, 2);

    $self->x = 3;
    $test->last;


    while (1) {
      1;
    }
    "abcde";
    "";

    # _sum();
    # sum_foo();
    
    # $title->[3];
    # $title->[3] = 4;

    my $title8 : boolean = true;
    my $title9 : boolean = false;

    # sum();
    # sum(1, 3, 5);

    my $char : char = 'a';
  }
  
  sub sum5($self : Test::Foo::Point3D) : int {
    $self->x;
  }
}
